micronaut:
  application:
    name: news-ingestion-service
  server:
    port: 8081

opensearch:
  url: http://localhost:9200
  index: news_article

ingestion:
  scheduler:
    enabled: true
    cron: "0 0 8 * * ?"
